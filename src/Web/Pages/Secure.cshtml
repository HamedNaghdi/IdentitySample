@page

@using Microsoft.AspNetCore.Authentication;

@model Web.Pages.SecureModel
@{
}

<h2 class="uppercase">this is secure page</h2>

<h3>Claims:</h3>
<dl>
    @foreach(var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>